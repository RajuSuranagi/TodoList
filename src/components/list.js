import React, { Component } from 'react';

class List extends Component {

    constructor(props){
        super(props);
    }

    render(){
        return (
            <div>
                This is the list that will be displayed by default
            </div>
        );
    }
}

export default List;